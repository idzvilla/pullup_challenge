# Чеклист после деплоя на Railway

## ✅ Шаг 1: Проверка деплоя

1. В Railway откройте сервис `pullup_challenge`
2. Перейдите на вкладку **"Deployments"**
3. Убедитесь, что последний деплой имеет статус **"SUCCESS"** (зеленая галочка)
4. Если статус "FAILED" - проверьте логи (кнопка "View logs")

## ✅ Шаг 2: Проверка переменных окружения

В Railway → сервис `pullup_challenge` → вкладка **"Variables"** проверьте наличие:

- ✅ `TELEGRAM_BOT_TOKEN` - токен вашего бота от @BotFather
- ✅ `DATABASE_URL` - должен быть автоматически установлен Railway (если PostgreSQL добавлен)
- ✅ `CHALLENGE_START_DATE` - `2025-12-01` (опционально, есть значение по умолчанию)
- ✅ `CHALLENGE_END_DATE` - `2026-11-30` (опционально, есть значение по умолчанию)
- ✅ `CHALLENGE_TARGET` - `18250` (опционально, есть значение по умолчанию)
- ✅ `REMINDER_TIME` - `09:00` (опционально, есть значение по умолчанию)

**Если `DATABASE_URL` отсутствует:**
1. Убедитесь, что PostgreSQL добавлен в проект
2. Railway → проект → "+ New" → "Database" → "Add PostgreSQL"
3. Переменная `DATABASE_URL` установится автоматически

## ✅ Шаг 3: Проверка логов

1. В Railway → сервис → вкладка **"Logs"**
2. Проверьте, что видите:
   - `База данных инициализирована успешно`
   - `Бот запущен`
   - `Напоминания настроены на ... UTC`
3. Если есть ошибки - скопируйте их и проверьте

## ✅ Шаг 4: Тестирование бота в Telegram

1. Найдите вашего бота в Telegram (по имени, которое вы дали @BotFather)
2. Отправьте команду `/start`
3. Бот должен ответить приветственным сообщением с кнопками

**Проверьте функционал:**
- ✅ Кнопка "⚡ +50" - добавляет 50 подтягиваний
- ✅ Кнопка "➕ Добавить подтягивания" - позволяет ввести любое число
- ✅ Кнопка "📊 Моя статистика" - показывает вашу статистику
- ✅ Кнопка "🏆 Лидерборд" - показывает топ-20 участников

## ✅ Шаг 5: Проверка базы данных

1. В Railway → PostgreSQL сервис → вкладка **"Data"**
2. Или используйте любой PostgreSQL клиент с `DATABASE_URL`
3. Проверьте, что таблицы созданы:
   - `users` - таблица пользователей
   - `pullups` - таблица записей подтягиваний

## ✅ Шаг 6: Проверка напоминаний

Напоминания будут отправляться ежедневно в указанное время (по умолчанию 09:00 UTC).

**Чтобы проверить работу напоминаний:**
- Подождите до времени напоминания
- Или временно измените `REMINDER_TIME` на ближайшее время для теста

## 🐛 Если что-то не работает

### Бот не отвечает
1. Проверьте логи в Railway
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
3. Проверьте, что деплой успешен

### Ошибки базы данных
1. Проверьте, что PostgreSQL добавлен
2. Убедитесь, что `DATABASE_URL` установлен
3. Проверьте логи на ошибки подключения

### Бот падает/перезапускается
1. Проверьте логи на ошибки
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что код закоммичен и запушен

## 📞 Полезные команды бота

- `/start` - начать работу с ботом
- `/stats` - показать статистику
- `/leaderboard` - показать лидерборд

## 🎉 Готово!

Если все шаги пройдены успешно - ваш бот работает! Теперь можно:
- Пригласить друзей в челлендж
- Начать записывать подтягивания
- Следить за прогрессом в лидерборде

